<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24">
  <!-- Rounded square outline (green border from main icon) -->
  <rect x="1" y="1" width="22" height="22" rx="5" ry="5" fill="none" stroke="currentColor" stroke-width="1.5"/>
  <!-- Chat bubble (upper-left) -->
  <ellipse cx="9" cy="9" rx="5" ry="4" fill="currentColor"/>
  <polygon points="7,13 6,16 10,13" fill="currentColor"/>
  <!-- Terminal cursor dash (right side) -->
  <rect x="14" y="11" width="6" height="2" rx="1" fill="currentColor"/>
</svg>
